(()=>{let scheduled=false;const run=()=>{if(scheduled)return;scheduled=true;requestAnimationFrame(()=>{scheduled=false;const credits=document.querySelector('#passCredits'),price=document.querySelector('#passAmount'),button=document.querySelector('#sellPass');if(!credits||!price||!button)return;const validate=()=>{const value=Number(String(price.value||'').replace(',','.')),ok=Number.isFinite(value)&&value>0;price.required=true;button.disabled=!ok;price.classList.toggle('invalid',!ok)};if(!price.dataset.validation){price.dataset.validation='1';price.addEventListener('input',validate);credits.addEventListener('change',()=>requestAnimationFrame(validate))}validate()})};new MutationObserver(run).observe(document.body,{childList:true,subtree:true});run()})();
