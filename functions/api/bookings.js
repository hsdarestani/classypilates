const json=(body,status=200)=>new Response(JSON.stringify(body),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store'}});
const validEmail=v=>/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(String(v||''));
const ref=()=>`CP-${crypto.randomUUID().replace(/-/g,'').slice(0,8).toUpperCase()}`;
async function sendMail(env,to,subject,html){if(!env.RESEND_API_KEY)return;try{await fetch('https://api.resend.com/emails',{method:'POST',headers:{authorization:`Bearer ${env.RESEND_API_KEY}`,'content-type':'application/json'},body:JSON.stringify({from:env.MAIL_FROM||'Classy Pilates <booking@classypilates.de>',to:[to],subject,html})})}catch(_){}}
export async function onRequestGet({request,env}){if(!env.DB)return json({ok:false,code:'db_not_configured',bookings:[]},503);const url=new URL(request.url);const email=url.searchParams.get('email')?.trim().toLowerCase();const reference=url.searchParams.get('reference')?.trim().toUpperCase();if(!validEmail(email)||!reference)return json({ok:false,error:'invalid_lookup'},400);try{const booking=await env.DB.prepare(`SELECT b.reference,b.class_id,b.status,b.payment_state,b.created_at,c.name,c.starts_at,c.location_id,l.name AS studio_name,c.coach FROM bookings b JOIN classes c ON c.id=b.class_id JOIN locations l ON l.id=c.location_id WHERE lower(b.email)=? AND upper(b.reference)=? LIMIT 1`).bind(email,reference).first();if(!booking)return json({ok:false,error:'booking_not_found'},404);const credits=await env.DB.prepare(`SELECT COALESCE(SUM(delta),0) AS balance FROM credit_ledger WHERE lower(email)=?`).bind(email).first();return json({ok:true,bookings:[booking],credits:Number(credits?.balance||0)})}catch(error){return json({ok:false,error:'booking_query_failed'},500)}}
export async function onRequestPost({request,env}){if(!env.DB)return json({ok:false,code:'db_not_configured'},503);let body;try{body=await request.json()}catch(_){return json({ok:false,error:'invalid_json'},400)}const classId=String(body.classId||'');const email=String(body.email||'').trim().toLowerCase();const firstName=String(body.firstName||'').trim();if(!classId||!validEmail(email))return json({ok:false,error:'invalid_booking'},400);const reference=ref();const id=crypto.randomUUID();try{await env.DB.prepare(`INSERT INTO bookings(id,reference,class_id,email,first_name,status,payment_state) VALUES(?,?,?,?,?,'reserved','paid')`).bind(id,reference,classId,email,firstName).run();const row=await env.DB.prepare(`SELECT b.reference,b.class_id,b.status,c.capacity,c.reserved_count,(c.capacity-c.reserved_count) AS spots,c.name,c.starts_at,l.name AS studio_name,c.coach FROM bookings b JOIN classes c ON c.id=b.class_id JOIN locations l ON l.id=c.location_id WHERE b.id=?`).bind(id).first();sendMail(env,email,`Deine Classy Pilates Buchung · ${row.name}`,`<h2>Buchung bestätigt</h2><p>${firstName?`Hi ${firstName},`:''} dein Platz ist reserviert.</p><p><strong>${row.name}</strong><br>${row.starts_at}<br>${row.studio_name}<br>Coach: ${row.coach||'Classy Pilates'}</p><p>Buchungsnummer: <strong>${row.reference}</strong></p>`);return json({ok:true,booking:row},201)}catch(error){const message=String(error?.message||error);if(message.includes('NO_CREDIT'))return json({ok:false,error:'credit_required'},402);if(message.includes('CLASS_FULL'))return json({ok:false,error:'class_full'},409);if(message.includes('DUPLICATE_BOOKING'))return json({ok:false,error:'duplicate_booking'},409);if(message.includes('CLASS_NOT_AVAILABLE'))return json({ok:false,error:'class_not_available'},409);return json({ok:false,error:'booking_failed',details:message},500)}}
export async function onRequestDelete({request,env}){if(!env.DB)return json({ok:false,code:'db_not_configured'},503);let body;try{body=await request.json()}catch(_){return json({ok:false,error:'invalid_json'},400)}const reference=String(body.reference||'');const email=String(body.email||'').trim().toLowerCase();if(!reference||!validEmail(email))return json({ok:false,error:'invalid_request'},400);try{const before=await env.DB.prepare(`SELECT b.reference,c.name,c.starts_at,l.name AS studio_name FROM bookings b JOIN classes c ON c.id=b.class_id JOIN locations l ON l.id=c.location_id WHERE b.reference=? AND lower(b.email)=? AND b.status='reserved'`).bind(reference,email).first();const result=await env.DB.prepare(`UPDATE bookings SET status='cancelled',cancelled_at=CURRENT_TIMESTAMP WHERE reference=? AND lower(email)=? AND status='reserved'`).bind(reference,email).run();if(!result.meta?.changes)return json({ok:false,error:'booking_not_found'},404);if(before)sendMail(env,email,`Stornierung bestätigt · ${before.name}`,`<h2>Buchung storniert</h2><p>Deine Reservierung ${before.reference} für <strong>${before.name}</strong> am ${before.starts_at} wurde storniert. Der verwendete Credit wurde deinem Konto wieder gutgeschrieben.</p>`);return json({ok:true,reference})}catch(error){return json({ok:false,error:'cancel_failed',details:String(error?.message||error)},500)}}
